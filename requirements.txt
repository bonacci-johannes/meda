# This file lists usefull dependencies for a local test setup.
# The only missing dependencies are meda and meda_adpkd which always need to be installed explicitly,
# e.g. with
#   pip install -e source_adpkd/
#   pip install -e source_meda/

wheel==0.36.2
setuptools_scm==6.0.1
